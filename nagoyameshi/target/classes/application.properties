spring.application.name=nagoyameshi

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/springboot_nagoyameshi
spring.datasource.username=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.encoding=utf-8
spring.sql.init.mode=always

spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=a7bf04a2913bf5
spring.mail.password=5540790eea8a5f
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.servlet.multipart.max-file-size=5MB

# Spring Framework全体のログレベルを設定 (必要に応じて調整)
logging.level.root=WARN

# Spring Securityの詳細ログ出力を有効にする
logging.level.org.springframework.security=DEBUG

# Spring Webの詳細ログ出力を有効にする
logging.level.org.springframework.web=DEBUG

# HibernateのSQLクエリをデバッグ出力
logging.level.org.hibernate.SQL=DEBUG

# Hibernateのパラメータバインディング情報を出力
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ログをファイルに出力 (ファイル名を指定)
# logging.file.name=logs/spring-boot-application.log

# ログの標準出力を有効にする (開発中の確認用)
logging.file.path=logs